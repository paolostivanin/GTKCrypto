#include <gtk/gtk.h>


void
sign_file_cb (GtkWidget *btn __attribute__((__unused__)),
              gpointer user_data)
{
    // TODO before signing, another function to ask the user to select which key to use
    // TODO message dialog "signing for FPR user email"
    return;
}